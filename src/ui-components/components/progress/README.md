# Usage

```sh

const label = (
<Label filled={true} color="green">
Active
</Label>
);

<Progress progressWidth={500} percent={17} />

<Progress label={label} percent={17} />

<Progress progressWidth={500} percent={17} />
```
